import React from 'react';
import CollectionsList from '../../entities/collections/collectionsList';

const Home = () => {
  return (
    <section className="home">
      <CollectionsList />
    </section>
  );
};

export default Home;
